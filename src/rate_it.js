import books from "./books.js"

function addRatingsHumbleBundle() {
  // 1. Determine the type of bundle (books, movies, games)
  // 2. Retrieve content names and dom nodes from the page
  // 3. Lookup content ratings from third party providers
  // 4. Rewrite page by appending to dom nodes to add ratings
}

console.log("Running!")
console.log(books)
